import { Section } from "./Section";
import AddMessage from "./AddMessage";

export const Contact = () => {
  return (
    <Section sectionTitle="Contact" anchor="contact">
      <AddMessage />
    </Section>
  );
};
